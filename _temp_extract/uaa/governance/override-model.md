# Override Model

Override is the only mechanism that allows boundary expansion.

Requirements:

- explicit
- attributable
- scoped
- time-limited
- verifiable

Prohibited:

- silent expansion
- persistent override
- implicit behavior

Any expansion without override is non-conformant.
