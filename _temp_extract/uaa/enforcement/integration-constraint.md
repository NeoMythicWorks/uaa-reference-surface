# Integration Constraint

UAA applies only where execution can be intercepted.

Requirements:

- all execution passes through control points
- authorization is verified before execution
- no bypass paths exist

UAA integrates at execution, not at code level.
